
  <div id="myCarousel" class="carousel slide anuncio-principal" data-ride="carousel">
    <!-- Indicators -->
    <ol class="carousel-indicators">
      <li data-target="#myCarousel" data-slide-to="0" class="active"></li>
      <li data-target="#myCarousel" data-slide-to="1"></li>
      <li data-target="#myCarousel" data-slide-to="2"></li>
    </ol>

    <!-- Wrapper for slides -->
    <div class="carousel-inner">
      <div class="item active">
        <img src="/public/img/carousel1.jpg" alt="Los Angeles">
      </div>

      <div class="item">
        <img src="/public/img/carousel2.jpg" alt="Chicago">
      </div>

      <div class="item">
        <img src="/public/img/carousel3.jpg" alt="New York">
      </div>
    </div>

    <!-- Left and right controls -->
    <a class="left carousel-control" href="#myCarousel" data-slide="prev">
      <span class="glyphicon glyphicon-chevron-left"></span>
      <span class="sr-only">Previous</span>
    </a>
    <a class="right carousel-control" href="#myCarousel" data-slide="next">
      <span class="glyphicon glyphicon-chevron-right"></span>
      <span class="sr-only">Next</span>
    </a>
  </div>

  <hr>

  <h1>Últimas quedadas</h1>

  <div class="row">
    <div class="[ col-xs-12 col-sm-12 ]">
      <ul id="eventHolder" class="event-list">
      </ul>
    </div>
  </div>
  <hr>

  <h1>Welcome</h1>
  <p>Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do
    eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad
    minim veniam, quis nostrud exercitation ullamco laboris nisi ut
    aliquip ex ea commodo consequat. Excepteur sint occaecat cupidatat non
    proident, sunt in culpa qui officia deserunt mollit anim id est
    laborum consectetur adipiscing elit, sed do eiusmod tempor incididunt
    ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis
    nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo
    consequat.</p>
    <hr>
    <h3>Test</h3>
    <p>Lorem ipsum...</p>













  <script type="text/javascript">

  var meses = ['Enero', 'Febrero', 'Marzo', 'Abril', 'Mayo', 'Junio', 'Julio', 'Agosto', 'Septiembre', 'Octubre', 'Noviembre', 'Diciembre'];

  const myHeaders = new Headers();
  myHeaders.append('authorization', `Bearer ${localStorage.token}`)

  fetch('/api/event', {
    method: 'GET',
    headers: myHeaders
  })
  .then(res => res.json())
  .then(data => {
    data.events.map(event => {


      var item = document.createElement('li')
      item.innerHTML =
      `<time datetime=${event.date}>
      <br />
      <span class="day">${new Date(event.date).getDate() }</span>
      <span class="month">${meses[new Date(event.date).getMonth()] }</span>
      <span class="year">${new Date(event.date).getYear() }</span>
      </time>
      <img alt=${event.name} src=${event.picture} />
      <div class="info">
      <h2 class="title"><a href=${"/event?eventId="+event._id}>${event.name}</a></h2>
      <p class="desc">${event.description}</p>
      <ul>
      <li style="width:34%;"><small><a href=${"user/"+event.user.nickname}><span class="glyphicon glyphicon-user"></span>${event.user.displayName}</a></small></li>
      <li style="width:33%;"><span class="label label-primary">${event.tag.name}</span></li>
      </ul>
      </div>`;

      document.getElementById('eventHolder').appendChild(item)
    })

  })

  </script>
